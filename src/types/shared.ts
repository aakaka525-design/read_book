/**
 * Shared Types (Deprecated)
 * @deprecated Use `src/types/core.ts` for pure types or `src/types/ui.ts` for UI types.
 * Keeping this for backward compatibility during refactor.
 */

export * from './core';
