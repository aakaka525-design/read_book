services:
  web:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      # Pass OPENAI_API_KEY from host .env
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
